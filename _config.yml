domain: shi00317.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://shi00317.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /REPOSITORY-NAME/
collections:
  authors:
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag